const config = {
    db: {
      /* don't expose password or any sensitive info, done only for demo */
      host: "localhost",
      user: "root",
      password: "taina2005",
      database: "casas",
    },
    listPerPage: 10,
  };
  module.exports = config;